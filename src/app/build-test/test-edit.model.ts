/*
    Used for annotations and selected complex words for the testing interface
*/
export interface testEntry {
    id?: string;
    word: string;
    annotation: string;
    document_id: string;
}


